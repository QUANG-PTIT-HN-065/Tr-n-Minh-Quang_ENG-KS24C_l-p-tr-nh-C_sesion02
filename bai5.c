#include<stdio.h>
int main(){
    // gán giá trị cho chiều dài vào chiều rộng hình chữ nhật
    int length = 10;
    int width = 20;
    // chu vi = (length + width) * 2 
    int perimeter = (length + width) * 2;
    // diện tích = length * width
    int acreage = length * width;
    return 0;
}