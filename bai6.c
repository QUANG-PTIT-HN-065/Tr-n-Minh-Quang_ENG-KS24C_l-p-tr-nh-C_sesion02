#include<stdio.h>
int main(){
    const float PI = 3.14;
    // r bán kính hình tròn
    float r = 5; 
    // chu vi hình tròn = 2 * PI * r
    float chu_vi = 2 * PI * r;
    // diện tích hình tròn = r * r * PI
    float dien_tich = r * r * PI;
   return 0;
}